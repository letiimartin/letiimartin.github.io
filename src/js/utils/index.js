// Exportar todas las utilidades desde un único archivo
import { formatCurrency } from './format.js';
import { validateField } from './validation.js';
import { getUrlParameter } from './url.js';
import { loadPropertyData, handleFormSubmit } from './form.js';

// Exportar todas las utilidades
export {
  formatCurrency,
  validateField,
  getUrlParameter,
  loadPropertyData,
  handleFormSubmit
};
